<%= simple_form_for [:admin, @teacher_assigment]  do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%=f.association :teacher, label_method: :first_name %>
      <%=f.association :course, label_method: :title %>
    </div>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
<% end %>