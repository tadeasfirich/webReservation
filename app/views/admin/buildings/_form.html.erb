<%= simple_form_for [:admin, @building]  do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%=f.input :title %>
      <%=f.input :code %>
    </div>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
<% end %>